version: '3.8'

services:
  cyber-anomaly-detector:
    build: .
    container_name: cyber_anomaly_app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    restart: unless-stopped